# SQL 注入 UNION 攻击：找出字符串兼容列

| 项 | 内容 |
|---|---|
| 来源 | PortSwigger Academy |
| 难度 | APPRENTICE |
| 模块 | SQL 注入 |
| 日期 | 待填写 |
| 耗时 | 待填写 |

> 这是一份**待填写**的骨架。做完题后把每一节补上，不要把这一段留着。

## 一、漏洞原理

待填写。提示：这一题是上一题（判断列数）的**下一步**。

- 知道了列数，为什么**还不能**直接读数据？
- `NULL` 为什么可以随便填进任何列都不报错？
- 如果把字符串 `'a'` 填进一个**数字类型**的列，数据库会怎么反应？为什么？

**关键理解**：这一题不是考"能不能注入"，而是考你能不能用**报错与否**这个信号，反推出**每一列的数据类型**。想清楚为什么必须先知道哪一列是文本类型 —— 后面读用户名/密码时才用得上。

## 二、复现步骤

待填写。把过程分成两段写：

**第一段：确认列数**（上一题的结果，简单带过）

- payload：
- 响应：

**第二段：逐列试字符串**（这一题的核心）

| 试的 payload | 响应状态 | 结论 |
|---|---|---|
| `UNION SELECT 'a',NULL,NULL--` | 待填写 | 待填写 |
| `UNION SELECT NULL,'a',NULL--` | 待填写 | 待填写 |
| `UNION SELECT NULL,NULL,'a'--` | 待填写 | 待填写 |

**第三段：把题目指定的字符串填进去**

- 题目给的随机串：待填写
- 最终 payload：待填写
- 结果（横幅是否变成 Solved）：待填写

**一定要写清楚**：为什么"200 和 500"这两个状态码本身就是**信息**。区分"注入失败"和"数据库在告诉你类型不对"。

## 三、根本原因

待填写。提示：和前面两题同源（用户输入被当成 SQL 代码执行），但这里多一个维度 ——

**为什么攻击者能"问"出列的类型？** 因为数据库对类型不匹配会**明确报错**。这个报错是给开发者的调试信息，但对攻击者来说是**免费的提示**。

想一层：如果这个应用把错误信息**完全吞掉**（永远返回同一个页面），这一题的思路还成立吗？

## 四、修复方案

待填写。提示方向：

- 参数化查询怎么同时防住这三题（001/002/003）？
- 除了修漏洞，**关闭详细错误回显**算不算一层防护？为什么它不能替代参数化查询，但值得做？

## 五、面试问答

待填写。可以先自己回答这几个：

- 怎么判断查询返回几列？几种方法，各有什么适用场景？
- 知道列数之后，为什么还必须找出字符串列？
- `UNION SELECT NULL,...` 里为什么先用 NULL 而不用具体值？
- 如果目标是 MySQL，`--` 后面为什么有时要有空格？

## 六、参考

- [Lab: SQL injection UNION attack, finding a column containing text](https://portswigger.net/web-security/sql-injection/union-attacks/lab-find-column-containing-text)
- [Examining the database in SQL injection attacks](https://portswigger.net/web-security/sql-injection/examining-the-database)

## 标签

`#SQL注入` `#UNION` `#列类型` `#报错信息`
